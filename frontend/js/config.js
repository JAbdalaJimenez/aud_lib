/**
 * AudioLib — Configuración global del Frontend
 * Este archivo centraliza la URL del backend para que funcione tanto localmente como en producción.
 */

// Cambiar a la URL de producción cuando esté desplegado (ej. 'https://aud-lib.onrender.com')
const API_URL = 'http://localhost:3001/api/books';

// Si querés desplegarlo, descomentá la de abajo y comentá la de arriba:
// const API_URL = 'https://aud-lib.onrender.com/api/books';
